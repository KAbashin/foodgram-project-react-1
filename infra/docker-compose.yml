#version: '2'
#services:
#  db:
#    image: postgres:13.0-alpine
#    volumes:
#      - /var/lib/postgresql/data/
#    env_file:
#      - ./.env
#
#  backend:
#    image: romangrbr/foodgram_backend:latest
##    build:
##      context: ../backend/foodgram
##      dockerfile: Dockerfile
#    restart: always
#    volumes:
#      - static_value:/app/static/
#      - media_value:/app/media/
#    depends_on:
#      - db
#    env_file:
#      - ./.env
#
#  frontend:
#    image: romangrbr/foodgram_frontend:latest
##    build:
##      context: ../frontend
##      dockerfile: Dockerfile
##    volumes:
##      - ../frontend/:/app/result_build/
#    volumes:
#      - ./frontend/:/app/result_build/
#    depends_on:
#      - backend
#
#  nginx:
#    image: nginx:1.21.3-alpine
#    ports:
#      - "80:80"
#    volumes:
#      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
#      - ./frontend/build:/usr/share/nginx/html/
#      - ./docs/:/usr/share/nginx/html/api/docs/
#      - static_value:/var/html/static/
#      - media_value:/var/html/media/
#    depends_on:
#      - frontend
#
#volumes:
#  static_value:
#  media_value:
#  db_value:

version: '3.3'
volumes:
  postgres_data:
  static_value:
  media_value:
services:
  db:
    image: postgres:12.4-alpine
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env

  # Поскольку у нас нет необходимости каждый раз пересобирать фронт, то воспользуемся заранее загруженным образом с хаба
  frontend:
    image: romangrbr/foodgram_frontend:latest
    volumes:
      - ../frontend/:/app/result_build/

  nginx:
    image: nginx:1.19.3
    ports:
      - 80:80
    volumes:
      - static_value:/static/
      - media_value:/media/
      - ./nginx.conf:/etc/nginx/templates/default.conf.template
      - ../frontend/build:/usr/share/nginx/html/
      - ../docs/redoc.html:/usr/share/nginx/html/api/docs/redoc.html
      - ../docs/openapi-schema.yml:/usr/share/nginx/html/api/docs/openapi-schema.yml
    restart: always
    depends_on:
      - back
      - frontend
    env_file:
      - ./.env

  back:
    image: romangrbr/foodgram_project
    restart: always
    volumes:
      - static_value:/code/static
      - media_value:/code/media
    depends_on:
      - db
      - redis
    env_file:
      - ./.env

  redis:
    image: redis:6.2.5